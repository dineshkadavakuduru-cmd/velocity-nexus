# Socket.IO server URL for multiplayer
# Leave empty for single-player only (no socket connection attempt)
# In production, set this to your deployed server URL (Railway/Render)
# Example: NEXT_PUBLIC_SOCKET_URL=https://velocity-nexus-server.up.railway.app
NEXT_PUBLIC_SOCKET_URL=
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001
